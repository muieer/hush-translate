# 背景

## 基于开源项目

HushTranslate 的首次提交约 99% 继承自开源项目 `fastZhe/bob`，应视为上游历史代码基线，而非本项目既定设计；若用户需求与首次提交中的实现冲突，以当前需求为准，不要将其理解为用户否定自己过去的决策。

# 开发与验证

## macOS 应用测试边界

Codex 可以自行执行编译、构建、自动化测试和命令行验证。

构建完成后可以启动 macOS App，但启动之后由用户负责实际操作和人工验收。除非用户明确要求，否则不要使用 Computer Use 或其他 GUI 自动化方式继续操作或测试 App。

对于必须通过实际操作才能验证的功能，说明需要用户手动验证的内容和预期结果即可。