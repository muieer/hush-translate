# 开发与验证

## macOS 应用测试边界

Codex 可以自行执行编译、构建、自动化测试和命令行验证。

构建完成后，Codex 可以启动 macOS App。启动后，由用户负责实际操作和人工验收。除非用户明确要求，否则不要使用 Computer Use 或其他 GUI 自动化方式继续操作或测试 App。

对于必须通过实际操作才能验证的功能，说明用户需要手动验证的内容和预期结果即可。

# 文档

撰写中文技术文档时，调用 `tech-doc-style-chinese` skill 优化表达。

# Git 版本控制

## 版本号变更

发布新版本时，同步更新 `document` 目录中的 `CHANGELOG` 文件。
