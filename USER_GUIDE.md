# HushTranslate 使用说明

HushTranslate 是一款常驻 macOS 菜单栏的翻译工具，支持选中文本、截图区域和剪贴板内容翻译。翻译由用户配置的 OpenAI 兼容模型服务完成。

## 使用前准备

- 系统要求：macOS 14.0 或更高版本。
- 启动后，HushTranslate 以气泡图标显示在菜单栏，不会打开主窗口。
- 点击菜单栏图标并进入“设置…”，完成模型服务、语言、快捷键和权限配置。设置会自动保存。

## 模型服务设置

在“设置… → 通用”的“OpenAI 兼容接口”中填写：

- **Base URL**：OpenAI 兼容接口的基础地址，末尾通常包含 `/v1`。
- **API Key**：服务商提供的密钥；本地模型服务按其要求填写。
- **Model**：接口使用的模型名称。
- **源语言与目标语言**：源语言可设为自动检测，目标语言需指定具体语言。

## 翻译功能

三项功能都可从菜单栏启动，也可使用全局快捷键：

| 功能 | 默认快捷键 | 操作方式 |
| --- | --- | --- |
| 开启翻译会话 | `⌃⌥⌘D` | 先开启会话，再在其他应用中拖动划词、双击选词或 Shift 点击扩选。当前默认允许未来 3 次有效划词；再次按快捷键重置为 3 次，不立即翻译当前选区。 |
| 截图翻译 | `⌘⌥⇧S` | 触发后拖动框选屏幕区域；按 `Esc` 取消。 |
| 剪贴板翻译 | `⌃⌥⌘V` | 先将文本复制到剪贴板，再触发翻译。 |

可在“设置… → 快捷键”中点击录制框，重新设置或清除快捷键。

## 截图翻译模式

在“设置… → 截图翻译”中可选择：

- **本地 Vision**：先使用 macOS Vision 识别文字。
- **多模态大模型**：直接由模型识别并翻译图片。
- **本地优先**：先使用 Vision；本地识别失败或结果为空时，改由模型识别图片。

当前版本的截图翻译最终都会把框选图片附加到模型请求中，因此所选模型必须支持图片输入，截图内容也会发送到所配置的模型服务。“本地 Vision”仅表示 OCR 先在本机执行，不代表整个截图翻译过程离线。

## 系统权限

- **辅助功能**：选中文本翻译需要此权限，用于模拟 `⌘C`。
- **屏幕录制**：截图翻译需要此权限，用于读取屏幕内容。

可在“设置… → 快捷键 → 权限”点击“去授权”打开对应的系统设置。授权后返回 HushTranslate 点击“重新检测”；辅助功能授权后若仍不能使用，请退出并重新启动应用。

剪贴板翻译不依赖上述两项权限。

## 翻译结果窗口

结果窗口显示翻译来源、模型、原文、译文和耗时，并支持：

- 选择并复制窗口中的文字；
- 一键复制原文或译文；
- 在窗口顶部调整源语言和目标语言，供后续翻译使用；
- 点击图钉使窗口保持在最前方；
- 点击“关闭”或按 `Esc` 关闭窗口。

内容较长时可在窗口内滚动。当前版本不保存翻译历史。

## 高级设置

“设置… → 高级”提供以下选项：

- **请求超时**：设置等待模型响应的最长时间。
- **自定义系统提示词**：整段作为 system 消息发送；支持 `{sourceLanguage}`、`{targetLanguage}` 和 `{input}` 三个变量，也可恢复默认内容。
- **检查权限**：重新检测辅助功能与屏幕录制权限。
- **显示数据目录**：在 Finder 中打开 HushTranslate 的应用数据目录。

## 常见问题

- **选中文本没有反应**：确认已授予辅助功能权限；部分浏览器、Electron 应用或远程桌面可能不响应模拟复制，可先手动复制，再使用剪贴板翻译。
- **截图无法开始**：确认已授予屏幕录制权限；授权后重新触发，必要时重启 HushTranslate。
- **截图识别成功但翻译失败**：确认模型支持图片输入，并检查 Base URL、API Key、Model 和网络连接。
- **接口报错或响应超时**：检查模型服务是否运行、接口是否兼容 `/chat/completions`，以及“高级”中的请求超时设置。
- **找不到应用窗口**：HushTranslate 是菜单栏应用，请在屏幕右上角的气泡图标中操作。

点击菜单栏中的“退出”可完全退出 HushTranslate。
